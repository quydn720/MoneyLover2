<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.moneylover2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MoneyLover2"
       >
        <activity
            android:name=".ui.ShowReportActivity"
            android:parentActivityName=".ui.MainActivity" />
        <activity
            android:name=".ui.NewTransactionActivity"
            android:parentActivityName=".ui.MainActivity" />
        <activity
            android:name=".ui.NewCategoryActivity"
            android:parentActivityName=".ui.MainActivity" />
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.MoneyLover2.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>

<!--    android:fullBackupContent="@xml/backup_descriptor"-->